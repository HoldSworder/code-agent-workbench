---
description: 控制上下文消耗，避免读取完整大文件导致对话轮次不足
alwaysApply: true
---

# 上下文效率规范

## 读取文件

- **禁止一次性读取超过 200 行的文件**。先用 Grep 定位关键行号，再用 `offset/limit` 只读取相关片段（50-100 行）。
- 需要理解文件全貌时，先读取前 30 行（imports + 导出签名），再按需读取具体函数。
- 多个文件需要检查时，优先用 Grep 跨文件搜索关键词，只对命中的文件做局部读取。

## 思考链

- `sequential-thinking` 单次任务不超过 3-4 轮。如果 3 轮内无法得出结论，说明信息不足，应先补充搜索而非继续推测。
- 禁止在 thinking 中反复验证同一结论。得出结论后立即行动。

## 委托子任务

- 当需要调查多个不相关的代码路径时（如同时查前端和后端），使用 Task agent 并行委托，避免大量代码塞入主上下文。
- 子任务应明确指定：要查什么、在哪个目录、返回什么结论。

## git diff

- 优先对单个文件运行 `git diff`，确认相关性后再查看其他文件。
- 避免一次性对所有修改文件运行 diff。
